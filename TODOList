- FileParser (regex is done, a few methods is lacking)
- StateDumper (dumping current machine state to txt file)
- join FileParser and StateDumper with API
- adapt the whole project to StateWatcher class
- MAKEFILE (in the future - cmake)
- a bunch of integration tests
